---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: entrasecuritygroups.iam.entra.governance.com
spec:
  group: iam.entra.governance.com
  names:
    kind: EntraSecurityGroup
    listKind: EntraSecurityGroupList
    plural: entrasecuritygroups
    singular: entrasecuritygroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current phase of the EntraSecurityGroup
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: The age of the EntraSecurityGroup
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: The ID of the EntraSecurityGroup in Entra
      jsonPath: .status.id
      name: ID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EntraSecurityGroup is the Schema for the entrasecuritygroups
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EntraSecurityGroupSpec defines the desired state of EntraSecurityGroup
            properties:
              description:
                type: string
              forProvider:
                properties:
                  credentialSecretRef:
                    type: string
                  serviceAccountRef:
                    type: string
                type: object
              groupTypes:
                items:
                  type: string
                type: array
              mailEnabled:
                default: false
                type: boolean
              mailNickname:
                type: string
              members:
                items:
                  properties:
                    id:
                      type: string
                    type:
                      enum:
                      - User
                      - Group
                      - ServicePrincipal
                      type: string
                  required:
                  - id
                  - type
                  type: object
                type: array
              name:
                maxLength: 256
                minLength: 1
                pattern: ^[^<>%&:\\?\/\*]+$
                type: string
              owners:
                items:
                  properties:
                    id:
                      type: string
                    type:
                      enum:
                      - User
                      - Group
                      - ServicePrincipal
                      type: string
                  required:
                  - id
                  - type
                  type: object
                type: array
              securityEnabled:
                default: true
                type: boolean
            required:
            - name
            type: object
          status:
            description: EntraSecurityGroupStatus defines the observed state of EntraSecurityGroup
            properties:
              conditions:
                description: Conditions of the EntraSecurityGroup.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              displayName:
                description: DisplayName is the display name of the EntraSecurityGroup.
                type: string
              id:
                description: ID is the unique identifier of the EntraSecurityGroup
                  in the external system.
                type: string
              memberGroups:
                description: groups as members of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              memberServicePrincipals:
                description: SPA as members of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              memberUsers:
                description: Users as members of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the latest observed generation
                  of the resource.
                format: int64
                type: integer
              ownerGroups:
                description: groups as Owners of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              ownerServicePrincipals:
                description: SPA as Owners of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              owners:
                description: Users as Owners of the EntraSecurityGroup.
                items:
                  type: string
                type: array
              phase:
                description: Phase represents the current phase of the EntraSecurityGroup.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
